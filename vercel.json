{
  "builds": [
    { "src": "apps/web/next.config.js", "use": "@vercel/next" }
  ],
  "routes": [
    { "src": "/(.*)", "dest": "apps/web/$1" }
  ],
  "buildCommand": "npm install && npm run build"
}
