services:
  postgres:
    image: postgres:16
    environment:
      POSTGRES_DB: omnitoolset
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres
    ports:
      - "5432:5432"
    volumes:
      - omnitoolset-postgres:/var/lib/postgresql/data

volumes:
  omnitoolset-postgres:
